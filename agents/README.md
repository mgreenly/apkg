# Agents Directory

This directory contains the agent infrastructure for the apkg project.

## Structure

- **skills/** - Agent skills (reusable agent behaviors and workflows)
- **scripts/** - Automation scripts (TypeScript/Deno scripts for tasks)
- **commands/** - Slash commands (custom commands for CLI)

Each subdirectory contains a `.keep` file to preserve the directory structure in version control.
